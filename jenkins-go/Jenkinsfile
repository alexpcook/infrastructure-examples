pipeline {
	agent any
	stages {
		stage("build") {
			steps {
				dir("jenkins-go") {
					sh "go version"
					sh "go fmt"
					sh "go install"
				}
			}
		}
		stage("test") {
			steps {
				dir("jenkins-go") {
					sh "go test -v"
				}
			}
		}
	}
}
